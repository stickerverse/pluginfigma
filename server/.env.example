# Port for the server to listen on
PORT=3000

# Google Cloud credentials - either set this path or use Application Default Credentials
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-credentials-file.json

# Optional: If you want to directly provide an API key instead of using a credentials file
GOOGLE_CLOUD_API_KEY=your-api-key-here

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=chrome-extension://your-extension-id,http://localhost:3000
